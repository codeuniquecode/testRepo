<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Account Information</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body, html {
      height: 100%;
    }
    .centered-card {
      height: 100%;
    }
  </style>
</head>
<body>
<nav>
   <ol class="breadcrumb m-5">
    <li class="breadcrumb-item"><a href="/adminDashboard">Back to Dashboard</a></li>
    <li class="breadcrumb-item active" aria-current="page">Account Info</li>
  </ol>
</nav>
  <div class="container d-flex justify-content-center align-items-center centered-card">
   
    <div class="card p-4 shadow-lg" style="max-width: 500px; width: 100%;">
      <h3 class="text-center fw-bold mb-3">Account Information</h3>
     

      
     <form action="/viewAccount?_method=PATCH" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label class="form-label">Change QR</label>
        <input type="file" class="form-control" name="image"  onchange="previewImage(event)" required accept="image/*">
        <img id="qrPreview" src="<%=updated.qr%>" alt="QR Preview" class="img-thumbnail" style="max-width: 200px;">
      </div>
        <div class="mb-3">
          <label class="form-label">Bank Name</label>
          <input type="text" class="form-control" name="bankName" value="<%=updated.bankName%>" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Branch</label>
          <input type="text" class="form-control" name="branchInfo" value="<%=updated.branchInfo%>" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Account Name</label>
          <input type="text" class="form-control" name="accountName" value="<%=updated.accountName%>" required>
        </div>
        <div class="mb-3">
          <label class="form-label">Account Number</label>
          <input type="text" class="form-control" name="accountNumber" value="<%=updated.accountNumber%>" required>
        </div>
        <button type="submit" class="btn btn-primary w-100">Update</button>
      </form>
    </div>
  </div>
<script>
  function previewImage(event) {
    const preview = document.getElementById('qrPreview');
    const file = event.target.files[0];
    if (file) {
      preview.src = URL.createObjectURL(file);
    }
  }
</script>

</body>
</html>
