<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>All Contacts</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
      <script src="https://kit.fontawesome.com/2c8d038c04.js" crossorigin="anonymous"></script>
      <style>
        .btn:hover{
            background-color: #f8f9fa !important;
            color: #000 !important;
            transition: all 0.3s ease-in-out;
        }
      </style>
</head>
<body>
  <div class="container my-4">
    <h1 class="mb-4 text-center">Contact Messages</h1>
     <ol class="breadcrumb">
    <li class="breadcrumb-item"><a href="/adminDashboard">Back to Dashboard</a></li>
    <li class="breadcrumb-item active" aria-current="page">Contacts</li>
  </ol>
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
      <% if (contactInfo.length === 0) { %>
        <div class="col">
          <div class="alert alert-info text-center">No contact messages found.</div>
        </div>
      <% } else { %>
        <% contactInfo.forEach(contact => { %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <p class="card-text"><strong>Name:</strong> <%= contact.name %></p>
                <p class="card-text"><strong>Email:</strong> <%= contact.email %></p>
                <p class="card-text"><strong>Phone:</strong> <%= contact.phone %></p>
                <p class="card-text"><strong>Subject:</strong> <%= contact.subject %></p>
                <p class="card-text"><strong>Message:</strong> <%= contact.message %></p>
                
                <p class="card-text"><strong>Date:</strong> <%= contact.date %></p>
              </div>
              <div class="card-footer text-muted btn text-center">
           <form action="/isRead/<%= contact._id %>" method="POST">
    <button type="submit" class="btn text-muted text-center">
        <i class="fa-solid fa-envelope-open"></i> Mark as read
    </button>
</form>

              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
