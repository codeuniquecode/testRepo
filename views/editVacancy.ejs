<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Announce Vacancy - Ashish Service Admin</title>
  <!-- Bootstrap 5 CSS CDN -->
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <style>
    body, html {
      height: 100%;
      background-color: #f8f9fa;
    }
    .vacancy-form-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .vacancy-form {
      background: white;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      max-width: 700px;
      width: 100%;
    }
    .form-header {
      margin-bottom: 25px;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="vacancy-form-container">
    <form class="vacancy-form" id="vacancyForm" action="/vacancy/<%=vacancyData._id%>?_method=PATCH" method="POST" >
      <h2 class="form-header">Announce New Vacancy</h2>

      <div class="mb-3">
        <label for="jobTitle" class="form-label">Job Title <span class="text-danger">*</span></label>
        <input type="text" name="title" class="form-control" id="jobTitle" value="<%=vacancyData.title%>" required />
        <div class="invalid-feedback">Please enter the job title.</div>
      </div>

      <div class="mb-3">
        <label for="department" class="form-label">Department <span class="text-danger">*</span></label>
        <input type="text" name="department" class="form-control" id="department" value="<%=vacancyData.department%>"  required />
        <div class="invalid-feedback">Please enter the department.</div>
      </div>

      <div class="mb-3">
        <label for="vacancyCount" class="form-label">Number of Vacancies <span class="text-danger">*</span></label>
        <input type="number" name="number" class="form-control" id="vacancyCount" min="1" value="<%=vacancyData.number%>"  required />
        <div class="invalid-feedback">Please enter a valid number of vacancies.</div>
      </div>

      <div class="mb-3">
        <label for="jobDescription" class="form-label">Job Description <span class="text-danger">*</span></label>
        <textarea class="form-control" name="description" id="jobDescription" rows="4"  required><%=vacancyData.description%></textarea>
        <div class="invalid-feedback">Please provide a job description.</div>
      </div>

      <div class="mb-3">
        <label for="qualifications" class="form-label">Required Qualifications <span class="text-danger">*</span></label>
        <textarea class="form-control" name="qualification" id="qualifications" rows="3"   required><%=vacancyData.qualification%></textarea>
        <div class="invalid-feedback">Please specify the required qualifications.</div>
      </div>

      <div class="mb-3">
        <label for="deadline" class="form-label">Application Deadline <span class="text-danger">*</span></label>
        <input type="date" class="form-control" name="deadline" id="deadline" value="<%= new Date(vacancyData.deadline).toISOString().split('T')[0] %>"  required />
        <div class="invalid-feedback">Please select an application deadline date.</div>
      </div>
<!-- 
      <div class="mb-3">
        <label for="contactEmail" class="form-label">Contact Email <span class="text-danger">*</span></label>
        <input type="email" class="form-control" id="contactEmail" placeholder="example@ashishservice.com" required />
        <div class="invalid-feedback">Please enter a valid email address.</div>
      </div> -->

      <div class="d-grid">
        <button type="submit" class="btn btn-primary btn-lg">Update Vacancy</button>
      </div>
    </form>
  </div>

  <!-- Bootstrap 5 JS Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- <script>
    // Bootstrap custom validation example
    (() => {
      'use strict';

      const form = document.getElementById('vacancyForm');

      form.addEventListener('submit', function (event) {
        if (!form.checkValidity()) {
          event.preventDefault();
          event.stopPropagation();
        } else {
          event.preventDefault(); // prevent actual submit for demo
          alert('Vacancy announced successfully!');
          form.reset();
        }
        form.classList.add('was-validated');
      });
    })();
  </script> -->
</body>
</html>
